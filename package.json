{
  "name": "lutron-pro-hub-mqtt",
  "version": "0.0.2",
  "description": "Connects the Lutron Pro Hub (Caseta or RA2 Select) with an MQTT broker.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.6",
    "mqtt": "^3.0.0"
  }
}
